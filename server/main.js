import { Meteor } from "meteor/meteor";
import { Accounts } from "meteor/accounts-base";
import { CoinTossesCollection } from "/imports/api/coinTosses";

Meteor.startup(() => {
  // CoinTossesCollection.remove({});
});
